import{_ as e,o as i,c as r,e as s}from"./app-OxDYV5f1.js";const t={},a=s(`<h1 id="system-library" tabindex="-1"><a class="header-anchor" href="#system-library" aria-hidden="true">#</a> System Library</h1><p>So far, we have three system calls: <code>print</code>, <code>yield</code>, and <code>exit</code>. It&#39;s going to be tedious to write everything in assembly, so we need to write a system library that wraps these system calls in procs that we can call from our user tasks. This library will be the interface between our user tasks and the kernel.</p><h2 id="writing-the-library" tabindex="-1"><a class="header-anchor" href="#writing-the-library" aria-hidden="true">#</a> Writing the Library</h2><p>Let&#39;s create a new top-level directory called <code>syslib</code>, and inside it, we&#39;ll create two modules for our system calls: <code>io.nim</code> (for <code>print</code>), and <code>os.nim</code> (for <code>yield</code> and <code>exit</code>).</p><p>Let&#39;s start with <code>io.nim</code>.</p><div class="language-nim line-numbers-mode" data-ext="nim"><pre class="language-nim"><code><span class="token comment"># src/syslib/io.nim</span>

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,6),o=[a];function d(n,c){return i(),r("div",null,o)}const m=e(t,[["render",d],["__file","22-system-library.html.vue"]]);export{m as default};
